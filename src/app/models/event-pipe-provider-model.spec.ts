import { EventPipeProviderModel } from './event-pipe-provider-model';

describe('EventPipeProviderModel', () => {
  it('should create an instance', () => {
    expect(new EventPipeProviderModel()).toBeTruthy();
  });
});
