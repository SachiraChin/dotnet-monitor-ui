import { ConfigurationService } from './configuration-service';

describe('ConfigurationService', () => {
  it('should create an instance', () => {
    expect(new ConfigurationService()).toBeTruthy();
  });
});
